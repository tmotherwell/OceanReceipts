{
  "name": "wave-enter-expense-playwright",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node playwright/enter_expenses.js",
    "install-browsers": "npx playwright install --with-deps",
    "setup": "npm install && npx playwright install --with-deps"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "playwright": "^1.35.0"
  }
}
